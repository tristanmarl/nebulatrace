syntax = "proto3";

package nebulatrace.hyperdrive;

service Hyperdrive {
  rpc Align(AlignRequest) returns (AlignReply);
}

message AlignRequest {
  string mission_id = 1;
  string mode = 2;
}

message AlignReply {
  string mission_id = 1;
  string status = 2;
  string message = 3;
}
